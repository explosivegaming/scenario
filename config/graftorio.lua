return {
  enabled=true, -- if the module is enabled at all
  server_save=true, -- save data only on server
  train_histogram_buckets="10,30,60,90,120,180,240,300,600", -- train histogram buckets
  graftorio_logistic_items=true
}